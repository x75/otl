function [ kstar ] = getKStar( x, PSOGP )
%GETKSTAR Summary of this function goes here
%   Detailed explanation goes here
    lenK = size(PSOGP.K,1);
    kstar = zeros(lenK,1);
    for i = 1:lenK    
        kstar(i) = PSOGP.covf(x, PSOGP.phi{i}, PSOGP.covf_params);
    end  
    
end

